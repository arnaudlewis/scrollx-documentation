doctype html(lang='en')

head
    title Scrollx Documentation
    link(rel="stylesheet", type="text/css", href="./stylesheets/compiled/scrollx.css")
    link(href="https://fonts.googleapis.com/css?family=Lato:300,400,700,900", rel="stylesheet", type="text/css")
    meta(name="viewport", content="width=device-width, initial-scale=1")

    // Required for Previews and Experiments
    script(src="//code.jquery.com/jquery-1.11.3.min.js")
    script(src="./javascripts/vendors/highlight.min.js")
    script(src="./javascripts/compiled/client.js")
    script
      = ctx?"window.prismic = { endpoint: '" + ctx.endpoint + "' };":""
    script(src="//static.cdn.prismic.io/prismic.min.js")
    script.
      window.Router = !{JSON.stringify(Router)}
body
    aside
        span MON MENU
    block body
